def pair_sum(arr,k):
    x=set()
    for i in range(0,len(arr)):
        for j in range(i+1,len(arr)):
            if(arr[i]+arr[j]==k):
               x.add((arr[i],arr[j]))
              
    return x    
p=list(pair_sum([1,3,2,2],4))
print('\n'.join(map(str,p)))
    
